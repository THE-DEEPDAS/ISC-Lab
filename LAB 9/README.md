# why choose e coprime with phi(n)
so modular inverse exist in Zn domain

# why modular inverse is necessary
for decryption

# what happens if m >= n
lose info while encryption

# textbook RSA is not secured
same Pt = same Ct so pattern 
vulnerable to CSA
if m is small then eth root is computable